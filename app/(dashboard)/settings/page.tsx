import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function SettingsPage(){const s=await prisma.businessSettings.findFirst();return <div className="max-w-3xl space-y-5"><div><h1 className="text-2xl font-semibold">Configuración</h1><p className="text-sm text-zinc-500 mt-1">Información general del negocio.</p></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">Barber Shop Central</h2><div className="grid sm:grid-cols-2 gap-4 mt-5 text-sm"><Info l="Teléfono" v={s?.phone||"—"}/><Info l="Correo" v={s?.email||"—"}/><Info l="Dirección" v={s?.address||"—"}/><Info l="Moneda" v={s?.currency||"USD"}/><Info l="Zona horaria" v={s?.timezone||"America/Caracas"}/><Info l="Intervalo de agenda" v={`${s?.appointmentSlot||30} minutos`}/></div></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">La configuración visual está lista; para editarla puedes conectar un formulario de actualización a <code className="font-mono">BusinessSettings</code>.</div></div>}
function Info({l,v}:{l:string;v:string}){return <div className="rounded-xl bg-zinc-50 p-3"><div className="text-[10px] uppercase tracking-wide text-zinc-400">{l}</div><div className="mt-1 font-semibold">{v}</div></div>}
